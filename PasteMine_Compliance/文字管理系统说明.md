# PasteMine 文字管理系统说明

## 📋 概述

为了便于后续调整字体、字号、中英文翻译等，新增了统一的文字管理系统。

---

## 📁 文件位置

**核心文件**：`PasteMine/PasteMine/Resources/Localizable.swift`

---

## 🏗️ 结构设计

### 主命名空间：`AppText`

所有文字按功能模块分组：

```swift
enum AppText {
    enum Settings { }      // 设置页面相关
    enum MainWindow { }    // 主窗口相关
    enum Onboarding { }    // 引导页面相关
    enum Notifications { } // 通知相关
    enum Menu { }          // 菜单相关
    enum Common { }        // 通用文字
}
```

---

## 📚 使用示例

### 设置页面

**之前：**
```swift
Text("设置")
Text("通知")
Text("复制时显示通知")
```

**现在：**
```swift
Text(AppText.Settings.title)
Text(AppText.Settings.General.notification)
Text(AppText.Settings.General.notificationDesc)
```

### 带参数的文字

**历史记录数量：**
```swift
// 之前
Text("\(count) 条")

// 现在
Text(AppText.Settings.Storage.historyCount(count))
```

---

## 🗂️ 完整分类

### 1. Settings（设置页面）

#### Settings.Groups（分组标题）
- `general` - "通用"
- `storage` - "存储"
- `privacy` - "隐私"

#### Settings.General（通用设置）
- `notification` - "通知"
- `notificationDesc` - "复制时显示通知"
- `sound` - "音效"
- `soundDesc` - "播放提示音效"
- `globalShortcut` - "全局快捷键"
- `globalShortcutDesc` - "显示/隐藏窗口"
- `launchAtLogin` - "开机自启动"
- `launchAtLoginDesc` - "自动启动应用"

#### Settings.Storage（存储设置）
- `historyLimit` - "历史记录上限"
- `historyLimitDesc` - "超出自动删除"
- `historyPermanent` - "永久"
- `historyCount(Int)` - 动态生成数量文字
- `ignoreLargeImages` - "忽略大图片以节省磁盘空间"
- `ignoreLargeImagesDesc` - "超过 20MB 的图片将不会被保存到历史中"

#### Settings.Privacy（隐私设置）
- `ignoreApps` - "忽略应用"
- `ignoreTypes` - "忽略类型"
- `selectApp` - "选择应用"
- `ignoreAppsDesc` - "这些应用中的复制操作不会被记录"
- `addType` - "添加"
- `typeListTitle` - "类型列表"
- `typePlaceholder` - "输入 pasteboard type"
- `ignoreTypesDesc` - "这些类型的内容不会被记录（如密码、临时数据）"
- `clearOnQuit` - "退出时清空剪贴板"
- `clearOnQuitDesc` - "退出应用时自动清除所有历史记录"
- `emptyList` - "列表为空"

### 2. MainWindow（主窗口）
- `windowTitle` - "剪贴板历史"
- `searchPlaceholder` - "搜索..."
- `emptyStateTitle` - "暂无剪贴板记录"
- `emptyStateMessage` - "开始复制内容，它们会出现在这里"
- `clearAll` - "清空"
- `settings` - "设置"

### 3. Common（通用）
- `delete` - "删除"
- `cancel` - "取消"
- `confirm` - "确认"
- `copy` - "复制"
- `paste` - "粘贴"

---

## 🎯 优势

### 1. 集中管理
- 所有文字在一个文件中
- 修改方便，不用到处找

### 2. 便于翻译
未来添加英文版只需：
```swift
enum AppText {
    static var language: Language = .chinese
    
    enum Settings {
        static var title: String {
            switch AppText.language {
            case .chinese: return "设置"
            case .english: return "Settings"
            }
        }
    }
}
```

### 3. 便于调整字号
可以在 `Localizable.swift` 中添加字体配置：
```swift
enum FontConfig {
    static let titleSize: CGFloat = 18
    static let bodySize: CGFloat = 14
    static let captionSize: CGFloat = 11
}
```

### 4. 类型安全
- 编译时检查
- 避免拼写错误
- IDE 自动补全

---

## 📝 迁移示例

### 完整示例：通知设置

**之前：**
```swift
SettingsSectionView(title: "通知") {
    VStack(alignment: .leading, spacing: 3) {
        Toggle("", isOn: $settings.notificationEnabled)
            .toggleStyle(.switch)
        
        Text("复制时显示通知")
            .font(.caption2)
            .foregroundStyle(.secondary)
    }
}
```

**现在：**
```swift
SettingsSectionView(title: "") {
    HStack(spacing: 12) {
        Text(AppText.Settings.General.notification)
            .font(.body)
        
        Spacer()
        
        Toggle("", isOn: $settings.notificationEnabled)
            .toggleStyle(.switch)
    }
    
    Text(AppText.Settings.General.notificationDesc)
        .font(.caption2)
        .foregroundStyle(.secondary)
        .padding(.top, 1)
}
```

---

## 🔮 未来扩展

### 1. 多语言支持
添加英文、日文等语言包

### 2. 动态字体
根据系统辅助功能自动调整字号

### 3. 主题支持
不同主题使用不同的文字风格

### 4. 可访问性
为视障用户提供更好的文字描述

---

## 🚀 快速参考

**修改某处文字：**
1. 打开 `Localizable.swift`
2. 找到对应的枚举和属性
3. 修改 `static let xxx = "新文字"`
4. 保存即可，所有使用该文字的地方自动更新

**添加新文字：**
1. 在合适的枚举下添加 `static let newText = "文字内容"`
2. 在需要的地方使用 `Text(AppText.模块.newText)`

---

## ✅ 当前已迁移的文件

- ✅ `SettingsView.swift` - 所有设置页面文字
- ✅ `AppPickerView.swift` - 应用选择器文字
- ✅ `EditableListView.swift` - 可编辑列表文字

---

## 📌 待迁移的文件

以下文件尚未迁移，可按需逐步迁移：
- `ContentView.swift`
- `HistoryListView.swift`
- `OnboardingView.swift`
- `EmptyStateView.swift`
- 其他包含硬编码文字的文件

迁移时遵循相同模式即可。
